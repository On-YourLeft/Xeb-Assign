//Program to check if a number is armstrong or not

#include <stdio.h>

int main()
{
    int number;
    printf("Enter the number:");
    scanf("%d",&number);
    int x;
    x=number%10;
    printf("%d",x);
}

